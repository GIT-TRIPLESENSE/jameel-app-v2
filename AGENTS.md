# AGENTS.md

This file is the persistent source of truth for future agents working in this repo. Update it whenever product, design, architecture, tooling, or workflow decisions change.

## 1. Project Overview

Jameel First App is a mobile-first React Native product surface for Jameel Motors in Italy. The August MVP is a guest-only EV Expert Hub focused on pre-sales discovery and evaluation for Geely and Zeekr, with no login, order tracking, or ownership features.

The app opens from a Jameel Motors umbrella Home Page, lets users choose Zeekr or Geely, then moves into a Jameel-owned brand space with four shallow tabs: Home, Product, Network, and Membership. Jameel Motors governs structure, trust, services, support, and network reassurance; Zeekr and Geely provide brand-specific content, models, imagery, accents, and metrics.

Canonical product and design context lives in `PRODUCT.md` and `DESIGN.md`.

## 2. Tech Stack & Versions

- Expo managed workflow: `expo ~56.0.12` (SDK 56)
- React Native: `0.85.3`
- React: `19.2.3`
- TypeScript: `~6.0.3`, strict mode enabled
- Routing: Expo Router `~56.2.11`
- Client/UI state: Zustand `^5.0.14`
- Server/async state: TanStack Query `^5.101.1`
- Styling: local token-based theme layer in `src/theme`, Urbanist loaded through `@expo-google-fonts/urbanist`
- Icons: lucide-react-native with `react-native-svg`
- i18n: i18next `^26.3.2`, react-i18next `^17.0.8`, expo-localization `~56.0.6`
- Testing: Jest, jest-expo `~56.0.5`, @testing-library/react-native `^14.0.1`
- Tooling: ESLint 9 flat config, eslint-config-expo, Prettier, Husky, lint-staged

## 3. Architecture Principles

- Feature-based modules: product features live under `src/features/<feature-name>` and are driven by `PRODUCT.md`.
- Mock-first services: consumers depend on typed interfaces, not concrete implementations.
- Single backend boundary: the future Nest.js BFF is the only backend the app talks to. No external or third-party service clients are allowed in the mobile app.
- Tokens-only styling: UI code must use `src/theme` tokens for color, spacing, radius, typography, and layout values.
- i18n and RTL from day one: all display strings go through i18next, and directionality is centralized in `src/i18n/rtl.ts`.
- Shape before building: ambiguous product work should capture purpose, user state, content, edge cases, constraints, and success criteria before implementation.

## 4. Directory Map

- `app/`: Expo Router routes. The root route composes the Jameel Motors Home prototype.
- `assets/`: static assets generated by Expo plus brand assets under `assets/brand` for Jameel, Geely, and Zeekr.
- `PRODUCT.md`: product strategy, audience, MVP scope, information architecture, entities, flows, states, conversion strategy, roadmap, anti-references, and open decisions.
- `DESIGN.md`: UI source of truth for brand architecture, tokens, contrast, typography, spacing, components, layout, accessibility, responsive rules, copy rules, and release checklist.
- `.codex/skills/impeccable/`: local Impeccable design skill for future shaping, critique, audit, polish, and live visual iteration.
- `src/components/`: shared presentational components that are not owned by a feature.
- `src/components/navigation/bottom-navigation/`: shared bottom navigation shell and placeholder route screen for non-home routes.
- `src/features/`: feature modules driven by `PRODUCT.md`.
- `src/features/brand-products/`: brand-aware PLP for Zeekr/Geely, fed by the vehicle catalog service and themed through brand token modes.
- `src/features/dealer-network/`: brand-aware Network dealer/workshop locator for Zeekr/Geely, fed by the dealer network service and themed through brand token modes.
- `src/features/jameel-home/`: Jameel Motors umbrella Home prototype. Current direction is Concept 2 Guide-Oriented: extended image hero with primary CTA, immediate two-card Geely/Zeekr choice, separate full-width quiz entry point, EV journey path banners, knowledge-pill video stories, Membership continuity, and compact people-led network carousel.
- `src/hooks/`: shared hooks used across features.
- `src/i18n/`: i18next config, locale resources, and RTL helpers.
- `src/lib/`: provider composition, env access, query client setup, and framework glue.
- `src/services/`: service interfaces, provider, and mock implementation.
- `src/services/mock/`: mock services that satisfy the same contracts as the future BFF implementation.
- `src/store/`: Zustand client/UI state stores.
- `src/theme/`: placeholder design tokens and ThemeProvider.
- `__tests__/`: cross-cutting and route-level tests. Feature tests should live beside their feature when features exist.

## 5. Conventions

- Naming: use PascalCase for components and types, camelCase for functions and variables, and kebab-case for feature folders.
- Imports: use `@/...` for `src/...` and `@app/...` for route imports in tests or tooling. Avoid deep relative imports across ownership boundaries.
- Per-feature structure: use this default unless a feature needs less:
  - `src/features/<feature-name>/components`
  - `src/features/<feature-name>/hooks`
  - `src/features/<feature-name>/types.ts`
  - `src/features/<feature-name>/index.ts`
  - `src/features/<feature-name>/README.md`
- Component patterns: keep shared components presentational, token-styled, typed, and unaware of service implementations.
- Feature screen files should compose imported components, not define nested render components inline. For UI-heavy features, use `components/atoms`, `components/molecules`, and `components/organisms`; each component gets its own file.
- App shell navigation: `app/_layout.tsx` renders the shared bottom navigation on every route except `/`. The Jameel umbrella homepage must remain free of the bottom navigation.
- Brand-space routes use `/brands/[brand]/...`. The current PLP route is `/brands/[brand]/products`; bottom-nav links become brand-aware when the active route includes `zeekr` or `geely`.
- Non-home screens must reserve bottom content padding from the bottom-nav layout tokens because the nav is fixed over the route content.
- State rules: use Zustand for local client/UI state only. Use TanStack Query for async/server state, caching, retries, invalidation, and request lifecycle.
- Error handling: services return the normalized `ApiResponse<T>` shape from `src/services/types.ts`. UI-facing error copy must be localized.

## 6. Styling & Theming

- Tokens live in `src/theme/tokens.ts`.
- UI code must not use raw colors, spacing, radii, typography sizes, font weights, or layout dimensions.
- `src/theme/tokens.ts` now contains the first Jameel design-token pass from `DESIGN.md`: Jameel/Zeekr/Geely color roles, Urbanist font families, 4px spacing scale, radius scale, shadows, icons, CTA dimensions, layout dimensions, and the current typography scale: hero titles `32pt`, section titles `20pt`, card titles `16pt`, and card body text `14pt`. Titles use `Urbanist_700Bold`; body and descriptive text use `Urbanist_400Regular`. Standard elements use `16px` radius; CTAs use `100px` radius.
- Bottom navigation follows the current visual reference: five items (`Home`, `Our products`, `Our network`, `Support`, `Event`), white fully rounded container, `12px` item gap, dark 16px-rounded active state wide enough to avoid clipping labels, 24px line icons, and token-only sizing/color. TODO(product.md): reconcile this with the four-tab MVP IA if the product source of truth changes.
- Header brand selector controls on Jameel Home, Brand Products, and Brand Network must open the shared contextual selector with `Jameel`, `Zeekr`, and `Geely`; the current context is shown with a selected row and check icon. The header must stay visually identical across those pages: white 48px selector button with 100px radius, centered Jameel logo at `96x62`, and a 32px profile icon inside a 44px touch target. Selecting `Jameel` routes to `/`, while selecting Zeekr/Geely preserves the current section where possible (`/products` stays products, `/network` stays network).
- Brand PLP model cards follow `DESIGN.md`: full-bleed vehicle image card capped at 360px, favorite affordance, title/price over a bottom-to-top gradient scrim, glass metrics tray, and 48px white/outline CTAs. Keep `16px` between the title/price row, metrics tray, and CTA row; the metrics tray minimum height is `72px`. Do not use a flat opacity block over the vehicle image. PLP top chips are exactly `Vehicles` and `Services` at `32px` height, with `Vehicles` selected by default and `16px` below the headline; do not render a separate `Models` title above them, and do not replace them with technical filters like SUV/Electric unless product direction changes. Zeekr uses red accent for headline/filter/metrics and white text on the active chip; Geely uses blue for the headline brand word, filters, and metrics.
- Brand Network dealer/workshop cards follow the same brand-space shell as PLP: stable Jameel header, brand-aware background, manual search, active filter chips, dark network/detail cards with brand-accent iconography, and white/outline CTAs. The map is currently a mock selectable panel; do not introduce device location or a third-party map SDK without product approval.
- White text over imagery must stay AA-readable at `14pt` regular. Darken image overlays or gradient scrims when contrast is not sufficient; do not solve image contrast by increasing body text weight.
- Top-level Jameel Home sections use a fixed `32px` vertical gap.
- Jameel Home follows the Concept 2 Guide-Oriented architecture from `jameel-homepage-concept-2-shape.md`: use an immersive extended local-image hero with low-positioned copy, one primary white 48px CTA that anchors to the brand-choice section, an evident Jameel petrol gradient scrim moving from left to right over the current hero image, a white Jameel logo, and a white 48px `jameel` selector button in the hero. Keep Geely/Zeekr as two side-by-side vehicle-choice cards without visible internal CTAs immediately after the hero under `Our solution`, with no `Choose your first step` eyebrow. These cards use neutral dark card surfaces, official brand logos in a compact `24px`-high frame, `18px` brand headlines spaced `12px` from the logo, and isolated vehicle images positioned absolutely bottom-left so the vehicle front stays visible. Place a compact white guided banner below them with no icon, a light stroke, subtle bottom shadow, English quiz copy, and a right arrow CTA, then show compact EV journey path banners under `An ecosystem that always guides you`. In that path section, keep only the path banners beneath the title; each banner is full-viewport width with square outer edges, attached to the next, separated by angled geometric shapes, uses one unified Jameel Primary petrol fill, and the final banner also cuts into the following background with an angled bottom edge. Each banner has a short label, one-line title, two-line subtitle, and a representative icon on the right. Place the knowledge-pill video story section directly below those ecosystem banners on the page background, without a petrol section fill; it has no yellow eyebrow and its title is `EV expert hub` with contrast-safe title coloring on the light page. Its cards must render as a stacked swipe carousel: central active card, lateral cards partially visible behind it, parallax-style scale/rotation, and horizontal swipe changes the active card. Do not animate card-level opacity there; keep rendered cards fully opaque and darken lateral cards only with the internal image scrim. Do not render the `Jameel as guide` media/storytelling section. Membership must use the supplied community background image, hug-content height, no icon, no `MEMBERSHIP` eyebrow, only 32px `Experience` and `Benefit` tags above the title, body text below the title, and a white 48px CTA. The Membership tags are not filter chips: use `8px` radius, Jameel petrol `#21424E` text, and a solid opaque much lighter yellow background derived from the Jameel Accent. Place the `People by your side` section after Membership as a compact horizontal carousel inside a full-width Jameel petrol band, with the global 20pt section heading, a contrast-safe text `Our network` CTA, and Consultants, Dealers, and Customer care cards unless product direction changes again.
- Keep `src/theme/tokens.ts`, `DESIGN.md`, and component usage synchronized as visual decisions change.
- Theme access goes through `ThemeProvider` and `useAppTheme`.

## 7. Internationalization & RTL

- Locale resources live in `src/i18n/locales`.
- Add strings to locale files using stable semantic keys, not English sentence keys.
- Supported locales are declared in `src/i18n/resources.ts`.
- RTL detection and React Native direction sync live in `src/i18n/rtl.ts`.
- The initial market is Italy. Screens must keep language consistent within each locale. The scaffold currently includes English and Arabic placeholder resources; add Italian before product UI work depends on localized copy.
- The current Jameel Home prototype intentionally uses English copy per stakeholder direction. Italian localization remains TODO before Italy-market release content is finalized.

## 8. Services & Data Layer

- All app data access goes through `src/services`.
- `AppServices` is the consumer-facing contract. The current implementation is `mockServices`.
- `ApiClient` defines the future BFF boundary with a configurable base URL via `EXPO_PUBLIC_API_BASE_URL`.
- Concrete endpoints and DTOs remain TODO during feature shaping. The NestJS BFF is future scope and must remain the single backend boundary.
- `vehicleCatalog` and `dealerNetwork` are typed mock services used by feature screens. Consumers call `useServices()` through a TanStack Query hook; swapping mock to BFF must preserve the same service contracts.
- MVP entities from `PRODUCT.md` include Brand, Vehicle, Service, Dealer, Workshop, Lead, TestDriveRequest, BrandSwitchState, RoutePlannerSession, MembershipItem, and ContentItem.
- To add a new service later:
  1. Add typed DTOs and service interface methods in `src/services/types.ts` or a dedicated service contract file.
  2. Implement the mock in `src/services/mock`.
  3. Export through `src/services/index.ts`.
  4. Consume only through `useServices()` or injected `AppServices`.
  5. Add the BFF implementation behind the same interface later, with no consumer changes.

## 9. Testing Strategy

- Use Jest and @testing-library/react-native.
- Test behavior visible to users and contract boundaries, not implementation details.
- Route-level scaffold tests live in `__tests__`.
- The current route-level test verifies the Jameel Home prototype renders through i18n.
- Bottom navigation tests verify configured labels and navigation events while the homepage test verifies the nav is absent from `/`.
- Brand products tests verify the Zeekr/Geely PLP renders through the mock catalog service and that filter chips update visible vehicles.
- Dealer network tests verify the Zeekr/Geely Network tab renders through the mock dealer network service, filters dealer/workshop locations, and supports manual city search.
- Future feature tests should live beside the feature they cover.
- Service implementations should have contract tests once concrete service methods exist.

## 10. Commands

- Install: `npm install`
- Run: `npm run start`
- iOS: `npm run ios`
- Android: `npm run android`
- Web: `npm run web`
- Web export check: `npx expo export --platform web --output-dir dist-web --clear` (`dist-web/` is generated and ignored)
- Lint: `npm run lint`
- Typecheck: `npm run typecheck`
- Test: `npm test`
- Format: `npm run format`

## 11. How Future Phases Plug In

- `PRODUCT.md` drives features, flows, service contracts, DTOs, and copy requirements.
- `DESIGN.md` drives UI, design tokens, assets, typography, spacing, color, and component styling.
- This file MUST be updated as each phase lands so future agents inherit the current truth instead of guessing.

## 12. Branch And Collaboration Workflow

- Codex must never make code, documentation, asset, or configuration changes directly on `main`.
- Before starting any requested change, Codex must check the current branch. If the current branch is `main`, create or switch to a dedicated branch first.
- Use one branch per feature, fix, visual iteration, or implementation block. Prefer descriptive branch names with the `codex/` prefix, for example `codex/brand-plp-cards` or `codex/header-brand-logos`.
- Keep branch scope tight. Do not mix unrelated product work, visual polish, tooling updates, and documentation changes in the same branch unless the user explicitly groups them.
- Treat `main` as the shared integration baseline. Read from it when needed, but only update it when the user explicitly asks to align `main` with a specific completed branch or set of changes.
- When asked to align `main`, first confirm the intended source branch or change set, verify the working tree state, run the relevant checks, then merge or fast-forward the approved branch into `main` according to the repo workflow. Push `main` only when explicitly requested.
- If uncommitted changes already exist, Codex must preserve them, identify whether they belong to the current task, and avoid staging, reverting, or overwriting work from other people.
- Before committing or opening a PR, Codex must summarize the branch scope and verification results so collaborators can review without guessing.
