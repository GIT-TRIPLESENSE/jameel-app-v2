# dealer-network

Brand-aware dealer and workshop locator for the Network tab.

The feature consumes the typed `dealerNetwork` service through `useServices()` and TanStack Query. Mock data lives behind the same contract that the future BFF must satisfy.
