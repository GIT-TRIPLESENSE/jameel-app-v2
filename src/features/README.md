# features

Feature modules land here after `product.md`. Each feature should own its local components, hooks, services adapters, types, tests, and barrel export while using shared services through `src/services`.
